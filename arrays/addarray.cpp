#include <iostream>
#include <vector>
using namespace std;
vector<int> addarrays(vector<int> &a, vector<int> &b)
{
  int i = a.size() - 1;
  int j = b.size() - 1;
  int carry = 0;
  vector<int> ans;
  while (i >= 0 || j >= 0 || carry)
  {
  }
}
int main()
{
  int arr[] = {1, 3, 4, 5, 7};
  int arr2[] = {3, 5, 3, 7, 9};
  int x = 5, y = 5;
}